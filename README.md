# React Quiz App

A customized React quiz app based on the Academind starter project.

## Run locally

- Install dependencies: `npm install`
- Start dev server: `npm run dev`
- Build: `npm run build`

## GitHub Pages

This project is configured for GitHub Pages via GitHub Actions.

After pushing to `main`, enable Pages in repository settings:

- **Settings → Pages → Source: GitHub Actions**
